import { HomeScreenContent } from "@/components/home-screen-content";
import { Screen } from "@/components/screen";

const HomeScreen = () => {
  return (
    <Screen topInset>
      <HomeScreenContent />
    </Screen>
  );
};

export default HomeScreen;
