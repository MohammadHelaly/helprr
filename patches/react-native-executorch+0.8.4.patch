diff --git a/node_modules/react-native-executorch/common/rnexecutorch/utils/FrameProcessor.cpp b/node_modules/react-native-executorch/common/rnexecutorch/utils/FrameProcessor.cpp
index 48be098..92c7258 100644
--- a/node_modules/react-native-executorch/common/rnexecutorch/utils/FrameProcessor.cpp
+++ b/node_modules/react-native-executorch/common/rnexecutorch/utils/FrameProcessor.cpp
@@ -11,19 +11,39 @@ bool hasNativeBuffer(jsi::Runtime &runtime, const jsi::Object &frameData) {
   return frameData.hasProperty(runtime, "nativeBuffer");
 }
 
+bool hasPixelBuffer(jsi::Runtime &runtime, const jsi::Object &frameData) {
+  return frameData.hasProperty(runtime, "pixelBuffer");
+}
+
 } // namespace
 
 cv::Mat extractFrame(jsi::Runtime &runtime, const jsi::Object &frameData) {
-  if (!hasNativeBuffer(runtime, frameData)) {
-    throw RnExecutorchError(RnExecutorchErrorCode::InvalidUserInput,
-                            "FrameProcessor: No nativeBuffer found in frame");
+  if (hasNativeBuffer(runtime, frameData)) {
+    auto nativeBufferValue = frameData.getProperty(runtime, "nativeBuffer");
+    uint64_t bufferPtr = static_cast<uint64_t>(
+        nativeBufferValue.asBigInt(runtime).asUint64(runtime));
+
+    return extractFromNativeBuffer(bufferPtr);
   }
 
-  auto nativeBufferValue = frameData.getProperty(runtime, "nativeBuffer");
-  uint64_t bufferPtr = static_cast<uint64_t>(
-      nativeBufferValue.asBigInt(runtime).asUint64(runtime));
+  if (hasPixelBuffer(runtime, frameData)) {
+    auto pixelBuffer = frameData.getProperty(runtime, "pixelBuffer")
+                           .asObject(runtime)
+                           .getArrayBuffer(runtime);
+    auto width = frameData.getProperty(runtime, "width").asNumber();
+    auto height = frameData.getProperty(runtime, "height").asNumber();
+    auto bytesPerRow = frameData.getProperty(runtime, "bytesPerRow").asNumber();
+    if (bytesPerRow == 0) {
+      bytesPerRow = width * 4;
+    }
+
+    return cv::Mat(static_cast<int>(height), static_cast<int>(width), CV_8UC4,
+                   pixelBuffer.data(runtime), static_cast<size_t>(bytesPerRow));
+  }
 
-  return extractFromNativeBuffer(bufferPtr);
+  throw RnExecutorchError(
+      RnExecutorchErrorCode::InvalidUserInput,
+      "FrameProcessor: No nativeBuffer or pixelBuffer found in frame");
 }
 
 cv::Mat frameToMat(jsi::Runtime &runtime, const jsi::Value &frameData) {
diff --git a/node_modules/react-native-executorch/lib/module/modules/computer_vision/VisionModule.js b/node_modules/react-native-executorch/lib/module/modules/computer_vision/VisionModule.js
index 5774ad6..89f3b80 100644
--- a/node_modules/react-native-executorch/lib/module/modules/computer_vision/VisionModule.js
+++ b/node_modules/react-native-executorch/lib/module/modules/computer_vision/VisionModule.js
@@ -61,25 +61,20 @@ export class VisionModule extends BaseModule {
     return (frame, isFrontCamera, ...args) => {
       'worklet';
 
-      let nativeBuffer = null;
-      try {
-        nativeBuffer = frame.getNativeBuffer();
-        /**
+      /**
         Currently isMirrored is never set to true in VisionCamera.
         That's why we need to use our own property to determine if we need
         to mirror the results
-         */
-        const frameData = {
-          nativeBuffer: nativeBuffer.pointer,
-          orientation: frame.orientation,
-          isMirrored: isFrontCamera
-        };
-        return nativeGenerateFromFrame(frameData, ...args);
-      } finally {
-        if (nativeBuffer?.release) {
-          nativeBuffer.release();
-        }
-      }
+       */
+      const frameData = {
+        pixelBuffer: frame.getPixelBuffer(),
+        width: frame.width,
+        height: frame.height,
+        bytesPerRow: frame.bytesPerRow,
+        orientation: frame.orientation,
+        isMirrored: isFrontCamera
+      };
+      return nativeGenerateFromFrame(frameData, ...args);
     };
   }
 
diff --git a/node_modules/react-native-executorch/src/modules/computer_vision/VisionModule.ts b/node_modules/react-native-executorch/src/modules/computer_vision/VisionModule.ts
index f8724b1..69bff14 100644
--- a/node_modules/react-native-executorch/src/modules/computer_vision/VisionModule.ts
+++ b/node_modules/react-native-executorch/src/modules/computer_vision/VisionModule.ts
@@ -73,25 +73,20 @@ export abstract class VisionModule<TOutput> extends BaseModule {
     return (frame: Frame, isFrontCamera: boolean, ...args: any[]): TOutput => {
       'worklet';
 
-      let nativeBuffer: { pointer: bigint; release(): void } | null = null;
-      try {
-        nativeBuffer = frame.getNativeBuffer();
-        /**
+      /**
         Currently isMirrored is never set to true in VisionCamera.
         That's why we need to use our own property to determine if we need
         to mirror the results
-         */
-        const frameData = {
-          nativeBuffer: nativeBuffer.pointer,
-          orientation: frame.orientation,
-          isMirrored: isFrontCamera,
-        };
-        return nativeGenerateFromFrame(frameData, ...args);
-      } finally {
-        if (nativeBuffer?.release) {
-          nativeBuffer.release();
-        }
-      }
+       */
+      const frameData = {
+        pixelBuffer: frame.getPixelBuffer(),
+        width: frame.width,
+        height: frame.height,
+        bytesPerRow: frame.bytesPerRow,
+        orientation: frame.orientation,
+        isMirrored: isFrontCamera,
+      };
+      return nativeGenerateFromFrame(frameData, ...args);
     };
   }
 
